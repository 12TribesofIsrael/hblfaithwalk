# Each child folder is its own independent git repo — do NOT track them from
# the workspace umbrella, only their per-repo .git pushes to their own remotes.
AIconsultantforHmblzayy/
faithwalklivecom/
aibiblegospelscom/
faithwalkbook/
claude-memory-backup/

# OS / editor noise
Thumbs.db
.DS_Store
*.swp
.vscode/

# Transient or large files — re-evaluate per file if you want one tracked
v1.png
analytics-pull.json

# Temp HTML / scratch
_*.html
_scratch/
